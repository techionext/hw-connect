model user {
  id   String @id
  data Json

  type type @default(PURCHASE)

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@map("data")
}

enum type {
  INITIATE_CHECKOUT
  PURCHASE
}
